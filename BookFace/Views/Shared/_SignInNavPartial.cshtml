@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="@typeof(FriendshipsController).GetControllerName()" asp-action="@nameof(FriendshipsController.All)">Friendships</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="@typeof(ChatsController).GetControllerName()" asp-action="@nameof(ChatsController.Index)">Chats</a>
    </li>
}